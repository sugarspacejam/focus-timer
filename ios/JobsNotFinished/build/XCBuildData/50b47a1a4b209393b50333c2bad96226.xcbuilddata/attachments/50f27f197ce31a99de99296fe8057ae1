#!/bin/sh
set -e

WEB_ASSETS_DIR="$TARGET_BUILD_DIR/$UNLOCALIZED_RESOURCES_FOLDER_PATH/WebAssets"
mkdir -p "$WEB_ASSETS_DIR"

cp "$SRCROOT/../../index.html" "$WEB_ASSETS_DIR/index.html"
cp "$SRCROOT/../../kobe.png" "$WEB_ASSETS_DIR/kobe.png"

